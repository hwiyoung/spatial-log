# prod 프론트 컨테이너용 — 정적 SPA 서빙 (스택 nginx 가 frontend:3000 으로 프록시)
server {
    listen 3000;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: 알려진 파일이 없으면 index.html 로 폴백 (client-side routing)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
