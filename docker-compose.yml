services:
  app:
    build:
      context: .
      target: development
    ports:
      - "5174:5174"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev

  # Production build (optional)
  # app-prod:
  #   build:
  #     context: .
  #     target: production
  #   ports:
  #     - "80:80"
