# Spatial Log - Production Environment Variables
# 이 파일을 .env.prod로 복사하고 값을 채워넣으세요
# cp .env.prod.example .env.prod

# ============================================
# 필수 설정 - 반드시 변경 필요
# ============================================

# 서버 URL (프론트엔드 접속 주소)
# 예: https://demo.spatial-log.com 또는 http://your-server-ip
SITE_URL=https://your-domain.com

# PostgreSQL 비밀번호 (강력한 비밀번호로 변경)
# 생성 예시: openssl rand -base64 32
POSTGRES_PASSWORD=your-strong-password-here

# JWT 시크릿 키 (반드시 새로 생성)
# 생성 명령: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-here

# ============================================
# API 키 - 새로 생성 필요
# ============================================
# 아래 명령으로 생성 가능 (JWT_SECRET 사용):
# node -e "console.log(require('jsonwebtoken').sign({role:'anon',iss:'supabase-prod',iat:Math.floor(Date.now()/1000),exp:Math.floor(Date.now()/1000)+315360000}, 'YOUR_JWT_SECRET'))"

ANON_KEY=your-anon-key-here
SERVICE_ROLE_KEY=your-service-role-key-here

# 3D 변환 서비스 API 키 (nginx → converter 인증용)
# 생성 예시: openssl rand -hex 32
CONVERTER_API_KEY=your-converter-api-key-here

# ============================================
# 프론트엔드 환경 변수
# ============================================

# Supabase API URL (Kong API Gateway)
VITE_SUPABASE_URL=${SITE_URL}:8101
VITE_SUPABASE_ANON_KEY=${ANON_KEY}

# 3D 변환 서비스 URL
VITE_CONVERTER_URL=${SITE_URL}:8201

# Cesium Ion 토큰 (선택사항 - Cesium 3D Tiles 사용 시)
VITE_CESIUM_ION_TOKEN=

# Sentry DSN (선택사항 - 에러 모니터링)
# https://sentry.io 에서 프로젝트 생성 후 DSN 복사
VITE_SENTRY_DSN=

# ============================================
# 참고 사항
# ============================================
# 1. 개발 환경(.env)과 완전히 다른 키를 사용하세요
# 2. .env.prod 파일은 절대 git에 커밋하지 마세요
# 3. 서버에서 직접 생성하거나 GitHub Secrets로 관리하세요
